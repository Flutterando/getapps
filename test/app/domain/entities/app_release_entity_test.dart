import 'package:flutter_test/flutter_test.dart';

void main() {
  test('app release entity ...', () async {
    //  const release = AppReleaseEntity.empty();
    // print(release);
    // print(release.tagName);
    // print(release.runtimeType);
  });
}
