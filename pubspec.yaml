name: getapps
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  asp: ^2.0.3
  auto_injector: ^2.0.5
  flutter:
    sdk: flutter
  result_dart: ^1.1.1
  routefly: ^2.0.4
  uno: ^1.1.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
